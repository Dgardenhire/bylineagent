---
phase: 01-marketing-site
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
autonomous: false
requirements:
  - SITE-07
  - SITE-08

must_haves:
  truths:
    - "Site is deployed to a Vercel preview URL"
    - "All 7 sections are visible in the browser"
    - "No horizontal scroll at 375px viewport"
    - "DM Sans font renders (not system fallback)"
    - "Gold accent color (#C9A227) visible on CTAs and highlights"
    - "No purple gradients or 3-column icon grids anywhere"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel configuration (if needed)"
  key_links:
    - from: "Vercel deployment"
      to: "GitHub repo"
      via: "vercel link + vercel deploy"
      pattern: "vercel"
---

<objective>
Deploy the marketing site to Vercel and verify all sections render correctly in a real browser.

Purpose: A local build passing is not enough. This plan deploys to Vercel, verifies all 7 sections render, checks responsive behavior at 375px, and confirms the anti-AI design checklist passes in production.
Output: A live Vercel preview URL with the complete marketing site.
</objective>

<execution_context>
@/Users/damongardenhire/.claude/get-shit-done/workflows/execute-plan.md
@/Users/damongardenhire/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-marketing-site/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to Vercel and run automated checks</name>
  <files>src/app/page.tsx, src/app/layout.tsx</files>
  <read_first>
    src/app/page.tsx (verify all components are composed)
    src/app/layout.tsx (verify fonts and metadata)
    package.json (verify build script exists)
  </read_first>
  <action>
    1. From /Users/damongardenhire/Desktop/bylineagent/, commit any uncommitted changes:
       git add -A && git commit -m "feat: build all marketing sections" (if there are changes)
       git push origin main

    2. Link and deploy to Vercel:
       vercel link --yes
       vercel deploy --yes

       Capture the preview URL from the output.

    3. Run automated verification:
       - `npm run build` must exit 0 (confirms SSG works)
       - Check that all section IDs exist in the built output:
         grep -r "how-it-works" .next/server/ (or check source)
         grep -r "sample" .next/server/
         grep -r "compare" .next/server/
         grep -r "pricing" .next/server/
         grep -r "faq" .next/server/

    4. Start dev server for browser verification:
       npm run dev &
       Wait for "Ready" message.

    5. Use agent-browser to verify at desktop width:
       agent-browser open http://localhost:3000
       agent-browser wait --load networkidle
       agent-browser screenshot desktop-full.png
       agent-browser snapshot -i

    6. Verify at 375px mobile width:
       agent-browser eval 'window.innerWidth' (check current)
       agent-browser screenshot mobile-375.png --width 375

    7. Check for errors:
       agent-browser eval 'document.querySelector("[data-nextjs-dialog]") ? "ERROR_OVERLAY" : "OK"'
       agent-browser eval 'document.body.innerText.trim().length > 0 ? "HAS_CONTENT" : "BLANK"'

    8. Verify anti-AI checklist in DOM:
       agent-browser eval 'getComputedStyle(document.body).fontFamily.includes("DM Sans") ? "DM_SANS_LOADED" : "FALLBACK_FONT"'
       agent-browser eval 'document.body.innerHTML.includes("purple") || document.body.innerHTML.includes("violet") ? "FAIL_PURPLE" : "NO_PURPLE"'

    9. Close browser: agent-browser close
    10. Kill dev server.
  </action>
  <verify>
    <automated>cd /Users/damongardenhire/Desktop/bylineagent && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `vercel deploy` outputs a preview URL (https://*.vercel.app)
    - `npm run build` exits with code 0
    - Desktop screenshot shows all 7 sections
    - Mobile screenshot at 375px shows no horizontal scrollbar
    - agent-browser eval for font returns "DM_SANS_LOADED"
    - agent-browser eval for purple returns "NO_PURPLE"
    - agent-browser eval for content returns "HAS_CONTENT"
    - No Next.js error overlay detected
  </acceptance_criteria>
  <done>Site deployed to Vercel preview URL. Build passes. All 7 sections render. No errors detected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual design approval</name>
  <files>none</files>
  <action>
    Present the Vercel preview URL to the user for visual review. The user will verify:
    1. All 7 sections render and look professional
    2. Navigation smooth-scrolls correctly
    3. Sample Output tabs and FAQ accordion work
    4. Mobile responsive at 375px
    5. LINCHPIN attribution visible in footer
    6. Overall boutique-agency quality, not template-generator quality
  </action>
  <what-built>
    Complete BylineAgent marketing site deployed to Vercel with all 7 sections:
    1. Sticky navigation with smooth-scroll links
    2. Hero with tagline, $49/month price, and CTAs
    3. How It Works — 4 steps with icons
    4. Sample Output — 3 tabbed LinkedIn posts
    5. Compare — side-by-side table vs tools and ghostwriters
    6. Pricing — $49/mo card with features and CTA
    7. FAQ — 7 expandable items
    8. Footer — LINCHPIN attribution
  </what-built>
  <how-to-verify>
    1. Open the Vercel preview URL (provided in Task 1 output)
    2. Scroll through all sections — do they look professional, not "AI-generated"?
    3. Click nav links — do they smooth-scroll to the correct section?
    4. Click the sample post tabs — do they switch between 3 posts?
    5. Click FAQ items — do they expand/collapse?
    6. Resize browser to phone width (~375px) — does it reflow without horizontal scroll?
    7. Check footer — is "By LINCHPIN" visible?
    8. Overall: does this look like a site built by a boutique agency, not a template generator?
  </how-to-verify>
  <verify>User responds with "approved" or describes issues</verify>
  <done>User has approved the visual design of the marketing site.</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Vercel preview URL is live and accessible
- All 7 sections visible on page
- Navigation smooth-scrolls to sections
- Sample Output tabs switch content
- FAQ accordion expands/collapses
- Mobile responsive at 375px (no horizontal scroll)
- DM Sans font loads (not system fallback)
- No purple gradients anywhere
- LINCHPIN attribution in footer
- User has approved the design
</verification>

<success_criteria>
- Vercel preview URL deployed and live
- All Phase 1 success criteria met:
  1. `next build` zero errors
  2. All 7 sections render
  3. Responsive at 375px
  4. Anti-AI checklist passes
  5. CTA buttons exist (href="#")
  6. LINCHPIN attribution visible
  7. GitHub repo with code pushed
- User approves the visual design
</success_criteria>

<output>
After completion, create `.planning/phases/01-marketing-site/01-03-SUMMARY.md`
</output>
