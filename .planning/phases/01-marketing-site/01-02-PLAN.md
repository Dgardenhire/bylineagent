---
phase: 01-marketing-site
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/Nav.tsx
  - src/components/Hero.tsx
  - src/components/HowItWorks.tsx
  - src/components/SampleOutput.tsx
  - src/components/Compare.tsx
  - src/components/Pricing.tsx
  - src/components/FAQ.tsx
  - src/components/Footer.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04
  - SITE-05
  - SITE-06
  - SITE-07
  - SITE-08
  - SITE-09
  - SITE-10

must_haves:
  truths:
    - "Visitor sees hero with tagline, price ($49/month), and CTA button"
    - "Visitor sees 4 how-it-works steps with icons and descriptions"
    - "Visitor can switch between 3 sample LinkedIn posts via tabs"
    - "Visitor sees comparison table with 3 columns and 6 feature rows"
    - "Visitor sees pricing card with $49/mo, 6 features, and CTA"
    - "Visitor can expand/collapse 7 FAQ items"
    - "Footer shows LINCHPIN attribution"
    - "Navigation is sticky with smooth-scroll links"
    - "Site is left-aligned by default with varied section heights"
    - "No purple gradients, no 3-column icon grids, no generic hero copy"
  artifacts:
    - path: "src/components/Nav.tsx"
      provides: "Sticky navigation with smooth scroll"
      contains: "fixed"
    - path: "src/components/Hero.tsx"
      provides: "Hero section with tagline, price, CTAs, stats"
      contains: "HERO"
    - path: "src/components/HowItWorks.tsx"
      provides: "4-step process section"
      contains: "HOW_IT_WORKS_STEPS"
    - path: "src/components/SampleOutput.tsx"
      provides: "Tabbed sample posts display"
      contains: "useState"
    - path: "src/components/Compare.tsx"
      provides: "3-column comparison table"
      contains: "COMPARE"
    - path: "src/components/Pricing.tsx"
      provides: "Pricing card with features and CTA"
      contains: "PRICING"
    - path: "src/components/FAQ.tsx"
      provides: "Accordion FAQ section"
      contains: "useState"
    - path: "src/components/Footer.tsx"
      provides: "Footer with LINCHPIN attribution"
      contains: "LINCHPIN"
    - path: "src/app/page.tsx"
      provides: "Page composing all 7 sections"
      min_lines: 15
  key_links:
    - from: "src/components/Hero.tsx"
      to: "src/lib/content.ts"
      via: "import"
      pattern: "import.*HERO.*from.*content"
    - from: "src/components/SampleOutput.tsx"
      to: "src/lib/content.ts"
      via: "import"
      pattern: "import.*SAMPLE_OUTPUT.*from.*content"
    - from: "src/components/FAQ.tsx"
      to: "src/lib/content.ts"
      via: "import"
      pattern: "import.*FAQ_ITEMS.*from.*content"
    - from: "src/app/page.tsx"
      to: "src/components/"
      via: "component imports"
      pattern: "import.*from.*components"
---

<objective>
Build all 7 marketing sections as React components and compose them into the landing page.

Purpose: This is the actual marketing site — the thing that sells BylineAgent. Every section must use copy from src/lib/content.ts (never hardcoded), follow the anti-AI design checklist (no purple, no icon grids, DM Sans, left-aligned, varied heights), and be responsive at 375px.
Output: A complete single-page marketing site with Nav, Hero, HowItWorks, SampleOutput, Compare, Pricing, FAQ, and Footer.
</objective>

<execution_context>
@/Users/damongardenhire/.claude/get-shit-done/workflows/execute-plan.md
@/Users/damongardenhire/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-marketing-site/01-01-SUMMARY.md

Source prototype (reference for layout, spacing, interactions — NOT for inline styles):
@/tmp/bylineagent/ghostdraft_site_prototype.jsx
</context>

<interfaces>
<!-- From Plan 01 — these files will exist when Plan 02 executes -->

From src/lib/types.ts:
```typescript
export interface Brand { name: string; tagline: string; price: number; postsPerWeek: number; }
export interface HeroContent { badge: string; headline: string[]; goldLine: string; subtext: string; primaryCta: string; secondaryCta: string; stats: { value: string; label: string }[]; }
export interface Step { num: string; title: string; description: string; icon: string; }
export interface SamplePost { title: string; content: string; voiceTag: string; }
export interface SampleOutputContent { sectionLabel: string; heading: string; subtext: string; authorName: string; authorTitle: string; authorInitials: string; posts: SamplePost[]; }
export interface CompareRow { feature: string; selfServe: string; bylineAgent: string; ghostwriter: string; }
export interface CompareContent { sectionLabel: string; heading: string; subtext: string; footnote: string; rows: CompareRow[]; }
export interface PricingContent { sectionLabel: string; heading: string; subtext: string; priceNote: string; ctaText: string; ctaSubtext: string; features: string[]; comparisons: { label: string; value: string }[]; }
export interface FaqItem { question: string; answer: string; }
export interface FooterContent { tagline: string; disclaimer: string; attribution: string; }
export interface NavLink { label: string; href: string; }
```

From src/lib/content.ts:
```typescript
export const BRAND: Brand;
export const NAV_LINKS: NavLink[];
export const HERO: HeroContent;
export const HOW_IT_WORKS_STEPS: Step[];
export const SAMPLE_OUTPUT: SampleOutputContent;
export const COMPARE: CompareContent;
export const PRICING: PricingContent;
export const FAQ_ITEMS: FaqItem[];
export const FOOTER: FooterContent;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build Nav, Hero, HowItWorks, and Footer components</name>
  <files>src/components/Nav.tsx, src/components/Hero.tsx, src/components/HowItWorks.tsx, src/components/Footer.tsx</files>
  <read_first>
    /tmp/bylineagent/ghostdraft_site_prototype.jsx (layout structure, spacing, interactions for Nav, Hero, HowItWorks, Footer)
    src/lib/content.ts (content constants to import)
    src/lib/types.ts (type interfaces)
    src/app/globals.css (available Tailwind config)
    tailwind.config.ts (custom color tokens)
  </read_first>
  <action>
    Build 4 components using Tailwind CSS classes (NOT inline styles). All copy comes from content.ts imports.

    **src/components/Nav.tsx** (SITE-09):
    - Fixed position, top-0, z-50, full width
    - Background: bg-navy/92 with backdrop-blur-lg
    - Bottom border: 1px solid rgba(201,162,39,0.15)
    - Height: h-16, flex between logo and nav links
    - Logo: 28x28 rounded-md gold gradient square with "B" letter + "BylineAgent" text
    - Nav links: Map over NAV_LINKS, each is an anchor tag with smooth scroll behavior (`scroll-behavior: smooth` on html or use scrollIntoView)
    - Links: text-text-muted hover:text-gold transition-colors
    - CTA button: "Start Free Trial" gold gradient bg, dark text, links to #pricing
    - Mobile: Hide nav links at < 768px, show only logo + CTA (nav links hidden via `hidden md:flex`)
    - Add `scroll-smooth` to the html element via globals.css: `html { scroll-behavior: smooth; }`

    **src/components/Hero.tsx** (SITE-01):
    - Section: min-h-screen, flex items-center, bg-navy
    - Subtle radial gradient: `radial-gradient(ellipse at 30% 20%, rgba(201,162,39,0.08) 0%, transparent 50%)` as background-image (use inline style for this one gradient)
    - Padding: pt-32 pb-20 px-6 md:px-12
    - Content constrained to max-w-3xl (NOT centered — left-aligned per anti-AI rule)
    - Badge pill: inline-block, rounded-full, gold border/bg-opacity, DM Mono uppercase text "AI-POWERED LINKEDIN GHOSTWRITER"
    - H1: text-5xl md:text-6xl font-extrabold tracking-tight text-text-primary, lines from HERO.headline
    - Gold price line: text-gold
    - Subtext: text-lg text-text-muted max-w-xl
    - Two buttons: primary gold gradient "See a Sample" (href="#sample"), secondary outline "How It Works" (href="#how-it-works")
    - Stats row: flex gap-8 mt-12, each stat has gold value (text-2xl font-extrabold) + muted label (text-sm text-text-dim)

    **src/components/HowItWorks.tsx** (SITE-02):
    - Section: id="how-it-works", py-24 px-6 md:px-12, bg-navy-light
    - Centered header: DM Mono gold label "HOW IT WORKS" + heading "Four steps. Five minutes of setup."
    - 2-column grid on md+, single column on mobile: grid grid-cols-1 md:grid-cols-2 gap-6
    - Each step card: rounded-xl border border-border-subtle bg-white/[0.03] p-8 hover:border-gold/30 transition-colors
    - Step number in DM Mono gold + lucide icon (22px, gold, strokeWidth 1.75) — import { Mic, Target, PenTool, TrendingUp } from "lucide-react"
    - Map icon string to component: const iconMap = { Mic, Target, PenTool, TrendingUp }
    - Title: text-xl font-bold text-text-primary
    - Description: text-sm leading-relaxed text-text-muted
    - Max width container: max-w-4xl mx-auto

    **src/components/Footer.tsx** (SITE-10):
    - Footer element: py-12 px-6 md:px-12, bg-navy-footer, border-t border-border-subtle
    - Top row: flex justify-between items-center
    - Left: small gold "B" logo + "BylineAgent" in text-text-dim
    - Right: FOOTER.tagline in text-text-faint
    - Bottom: FOOTER.disclaimer in text-xs text-text-faint/70
    - LINCHPIN attribution: "By LINCHPIN" — text-xs text-text-dim, centered or right-aligned, clearly visible
    - Link "LINCHPIN" to https://linchpin.studio (gold text, subtle)
  </action>
  <verify>
    <automated>cd /Users/damongardenhire/Desktop/bylineagent && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Nav.tsx contains "fixed" and "NAV_LINKS" and "Start Free Trial" and "#pricing"
    - src/components/Hero.tsx contains "HERO" and "min-h-screen" and "#sample" and "#how-it-works"
    - src/components/HowItWorks.tsx contains "HOW_IT_WORKS_STEPS" and "lucide-react" and "id=\"how-it-works\""
    - src/components/Footer.tsx contains "FOOTER" and "LINCHPIN" and "linchpin.studio"
    - All 4 files import from "@/lib/content"
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Nav (sticky, smooth-scroll), Hero (tagline, price, CTAs, stats), HowItWorks (4 steps with icons), and Footer (LINCHPIN attribution) render correctly with all copy from content.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Build SampleOutput, Compare, Pricing, FAQ and compose page</name>
  <files>src/components/SampleOutput.tsx, src/components/Compare.tsx, src/components/Pricing.tsx, src/components/FAQ.tsx, src/app/page.tsx</files>
  <read_first>
    /tmp/bylineagent/ghostdraft_site_prototype.jsx (layout structure for SampleOutput, Compare, Pricing, FAQ)
    src/lib/content.ts (content constants)
    src/lib/types.ts (type interfaces)
    src/components/Nav.tsx (pattern reference for component structure)
    tailwind.config.ts (custom tokens)
  </read_first>
  <action>
    Build 4 components + update page.tsx. All copy from content.ts. Tailwind classes only (no inline styles except the Hero radial gradient).

    **src/components/SampleOutput.tsx** (SITE-03):
    - "use client" directive (uses useState for tab switching)
    - Section: id="sample", py-24 px-6 md:px-12, bg-navy
    - Centered header: DM Mono gold label + heading + subtext from SAMPLE_OUTPUT
    - Tab row: 3 buttons (flex, gap-2), each shows post title from SAMPLE_OUTPUT.posts
    - Active tab: bg-gold/15 border-gold/40 text-gold. Inactive: bg-white/[0.04] border-border-subtle text-text-muted
    - Post display card: rounded-xl border border-white/10 bg-white/[0.03] p-9
    - Author header: 44px circle with gradient bg (blue to indigo), initials, name + title
    - Post content: text-sm leading-7 text-gray-300 whitespace-pre-line
    - Voice tag pill: inline-block, bg-gold/[0.08] border border-gold/15 rounded-lg px-4 py-2.5, DM Mono text-xs text-gold
    - Max width container: max-w-3xl mx-auto

    **src/components/Compare.tsx** (SITE-04):
    - Section: id="compare", py-24 px-6 md:px-12, bg-navy-light
    - Centered header from COMPARE content
    - HTML table (NOT CSS grid per CLAUDE.md rule): width full, border-collapse
    - 4 columns: Feature | Self-Serve Tools | BylineAgent (highlighted) | Human Ghostwriter
    - BylineAgent column header: gold text, gold bottom border (2px), subtle gold bg
    - BylineAgent column cells: font-semibold text-text-primary, subtle gold bg
    - Other columns: text-text-muted, standard borders
    - Responsive: overflow-x-auto wrapper for mobile
    - Footnote below table: italic text-text-muted max-w-xl mx-auto
    - Max width container: max-w-4xl mx-auto

    **src/components/Pricing.tsx** (SITE-05):
    - Section: id="pricing", py-24 px-6 md:px-12, bg-navy-light
    - Centered header from PRICING content
    - Single pricing card: max-w-lg mx-auto, rounded-2xl, border border-gold/20, bg-gold/[0.04], p-12
    - Price: text-6xl font-extrabold text-text-primary + "/month" in text-text-dim
    - Price note: "Cancel anytime. 7-day free trial." text-text-muted
    - Feature list: flex flex-col gap-3.5, each with gold checkmark (unicode &#10003;) + text
    - CTA button: w-full, gold gradient bg, rounded-xl, py-4, text-lg font-bold, dark text
    - CTA subtext below button: text-sm text-text-dim text-center
    - Comparison row below card: flex justify-center gap-8 mt-10, each with gold value + dim label
    - CTA href="#" for now (wired to Stripe in Phase 2)

    **src/components/FAQ.tsx** (SITE-06):
    - "use client" directive (uses useState for accordion)
    - Section: id="faq", py-24 px-6 md:px-12, bg-navy
    - Centered header: DM Mono gold label "FAQ" + heading "Common questions"
    - Accordion: flex flex-col gap-0.5, max-w-2xl mx-auto
    - Each item: button full-width, text-left, py-5, border-b border-border-subtle
    - Question: text-base font-semibold text-text-primary
    - Toggle icon: "+" that rotates 45deg when open (transition-transform duration-200)
    - Answer panel: conditional render, py-3, text-sm leading-7 text-text-muted
    - Active item: bg-gold/[0.06]
    - Only one FAQ open at a time (click open closes previous)

    **src/app/page.tsx** — compose all sections:
    ```tsx
    import { Nav } from '@/components/Nav';
    import { Hero } from '@/components/Hero';
    import { HowItWorks } from '@/components/HowItWorks';
    import { SampleOutput } from '@/components/SampleOutput';
    import { Compare } from '@/components/Compare';
    import { Pricing } from '@/components/Pricing';
    import { FAQ } from '@/components/FAQ';
    import { Footer } from '@/components/Footer';

    export default function Home() {
      return (
        <>
          <Nav />
          <Hero />
          <HowItWorks />
          <SampleOutput />
          <Compare />
          <Pricing />
          <FAQ />
          <Footer />
        </>
      );
    }
    ```

    ANTI-AI DESIGN CHECKLIST (SITE-07):
    - NO purple or violet colors anywhere
    - NO 3-column icon grid (HowItWorks uses 2-column grid)
    - DM Sans font-family via Tailwind font-sans class
    - Left-aligned hero (not centered)
    - Varied section heights (Hero is min-h-screen, others are py-24)
    - At least one asymmetric moment (Hero is fully left-aligned while other sections have centered headers)
    - Body text 16px (text-base), limited to 3-4 text sizes
    - One accent color (gold) + warm neutrals + dark bg

    RESPONSIVE (SITE-08):
    - All sections use px-6 for mobile padding
    - Nav hides links on mobile, shows only logo + CTA
    - HowItWorks grid collapses to single column
    - Compare table uses overflow-x-auto
    - Hero headline: text-4xl on mobile (text-5xl md:text-6xl)
    - Pricing comparisons: flex-wrap on mobile
    - All max-w containers prevent content from stretching too wide
  </action>
  <verify>
    <automated>cd /Users/damongardenhire/Desktop/bylineagent && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SampleOutput.tsx contains "use client" and "useState" and "SAMPLE_OUTPUT" and "id=\"sample\""
    - src/components/Compare.tsx contains "COMPARE" and "<table" and "id=\"compare\"" and "overflow-x-auto"
    - src/components/Pricing.tsx contains "PRICING" and "$49" or "BRAND.price" and "id=\"pricing\"" and "Start My Free Trial" or "PRICING.ctaText"
    - src/components/FAQ.tsx contains "use client" and "useState" and "FAQ_ITEMS" and "id=\"faq\""
    - src/app/page.tsx imports all 8 components: Nav, Hero, HowItWorks, SampleOutput, Compare, Pricing, FAQ, Footer
    - No file contains "purple" or "violet" or "#7c3aed" or "#8b5cf6"
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All 7 marketing sections render on a single page. Content sourced from content.ts. Responsive at 375px. Anti-AI design rules followed. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- All 8 component files exist in src/components/
- src/app/page.tsx imports and renders all 8 components
- No purple/violet colors in any component
- All components import from "@/lib/content"
- "use client" directive present in SampleOutput.tsx and FAQ.tsx
- LINCHPIN attribution in Footer.tsx
- All section IDs present: how-it-works, sample, compare, pricing, faq
</verification>

<success_criteria>
- All 7 sections render: Hero, How It Works, Sample Output, Compare, Pricing, FAQ, Footer
- Navigation is sticky with smooth-scroll to section IDs
- Site is responsive — mobile uses single column layouts, overflow-x-auto on table
- Anti-AI checklist passes: no purple, no icon grids, DM Sans, left-aligned hero, varied heights
- CTA buttons exist (href="#" for now)
- LINCHPIN attribution visible in footer
- `next build` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-marketing-site/01-02-SUMMARY.md`
</output>
